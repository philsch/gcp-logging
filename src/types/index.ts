import type {StructuredLog} from "./StructuredLog";
import type {LogMessage} from "./LogMessage";
import {Severity} from './StructuredLog';

export type {StructuredLog, LogMessage};
export {Severity};